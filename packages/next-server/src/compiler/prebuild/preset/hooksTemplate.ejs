'use client'
import React from 'react'
import mi from '@/models/indexes.json'
import { createUseSignal } from '@polymita/next-connect'
import { createPlugin } from './connect'

export const useSignal = createUseSignal({
  modelIndexes: mi,  
  React: React,
  plugin: createPlugin(),
})
