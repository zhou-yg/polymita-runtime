const { contextBridge, ipcRenderer, IpcRendererEvent } = require('electron');

contextBridge.exposeInMainWorld('polymita', {
  nodejs: (module, api, args) => {
    return ipcRenderer.invoke('nodejs', module, api, args);
  },
  config: () => {
    return ipcRenderer.invoke('config');
  }
});
